export enum QuestionType {
    GROUP,
    DISPLAY,
    BOOLEAN,
    INTEGER,
    DECIMAL,
    DATE,
    DATETIME,
    STRING,
    CHOICE,
    PAGE,
    DROPDOWN
}

export enum EnableBehavior {
    ALL,
    ANY
}

export enum QuestionExtension {
  HIDDEN,
  PAGE,
  DROPDOWN
}
