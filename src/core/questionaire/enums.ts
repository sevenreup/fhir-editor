export enum QuestionType {
  GROUP = "group",
  DISPLAY = "display",
  BOOLEAN = "boolean",
  INTEGER = "integer",
  DECIMAL = "decimal",
  DATE = "date",
  DATETIME = "datetime",
  STRING = "string",
  CHOICE = "choice",
  PAGE = "page",
  DROPDOWN = "dropdown",
}

export enum EnableBehavior {
  ALL,
  ANY,
}

export enum QuestionExtension {
  HIDDEN,
  PAGE,
  DROPDOWN,
}
